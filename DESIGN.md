# 栈川行 · 第二版

本版以 iPhone 17 的 402 × 874 逻辑画布作为桌面交互原型。真实手机浏览器中隐藏设备外壳，使用全屏响应式布局。

## 视觉

- 雾白、石灰中性色与单一朱红强调色。颜色主要服务选中状态、主操作与路线。
- 原创 SVG 品牌符号、统一 1.65 线宽的矢量图标、矢量路线和影片概念封面；不依赖贴图或外部字体。
- 毛玻璃只用于悬浮底栏、卡片操作层与弹层。提供不支持背景模糊时的实色回退。
- 五个入口：发现、导览、光影、探索、我的。
- 动画遵循系统的减少动态效果偏好。弹层支持 Escape、焦点约束和键盘导航。

## 已实现

路线选择、展厅节点切换、讲解文字与浏览器朗读、搜索、收藏、本地足迹、机器人连接演示、投影控制演示、路线顺序小游戏、完整五题问答、矢量纪念卡下载、字号调整。

## 原型范围

场馆地图为示意图；机器人和投影为本地模拟状态；影片封面是设计概念，尚未接入实际影视文件。正式交付需要场馆实测地图、设备接口和已获授权的内容。

## 代码入口

`src/JourneyApp.tsx`：应用状态和设备原型。

`src/Pages.tsx`：五个主页面。

`src/Panels.tsx`：详情、设备、游戏与其他弹层。

`src/Visuals.tsx`：矢量图标与艺术图形。

`src/design.css`：视觉系统与响应式样式。

`src/content.ts`：路线、史实摘要、内容与题库。
